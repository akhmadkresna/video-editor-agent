ChapterMarker — a persistent corner label ("01 · The Setup") for splitting a long A-roll into chapters.

```jsx
<ChapterMarker number={1} title="The setup" corner="top-left" />
```

Always docked to a top corner inside the safe zone so it never overlaps the speaker's face.
