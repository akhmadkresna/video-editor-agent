/**
 * @startingPoint section="Overlays" subtitle="Full-bleed dashed grid + corner triangles — the design-canvas backdrop behind a beat" viewport="700x260"
 */
export interface AnnotationGridProps {
  /** Number of grid cells per axis (2 = one center cross, 3 = classic 3x3 rule-of-thirds) */
  density?: number;
}
