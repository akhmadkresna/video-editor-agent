AnnotationGrid — the full-bleed dashed rule-of-thirds grid with two corner triangle marks, lifted straight from the reference clips' "design canvas" look. Sits behind everything else, slowly pulsing, to make a beat feel like it's being actively sketched out live rather than static.

```jsx
<div style={{position:'relative',width:960,height:540}}>
  <AnnotationGrid density={3} />
  <PunchWord text="Business" cursor />
</div>
```
