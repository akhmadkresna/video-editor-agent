CalloutArrow — a dashed pointer with a dot origin and short label, annotating a specific spot in frame (a product, a gesture, a graph point).

```jsx
<CalloutArrow label="here" direction="up-right" length={80} />
```
