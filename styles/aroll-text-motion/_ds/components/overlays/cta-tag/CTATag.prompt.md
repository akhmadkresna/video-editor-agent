CTATag — the closing-beat pill ("Follow for part 2 →", "Link in bio →"). Solid white fill, dark text, no accent color.

```jsx
<CTATag label="Follow for part 2" variant="solid" position="bottom-center" />
```

`variant="outline"` for a quieter mid-video nudge; `solid` for the final-frame CTA.
