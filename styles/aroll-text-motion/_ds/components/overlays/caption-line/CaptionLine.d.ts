/**
 * @startingPoint section="Overlays" subtitle="Bottom-third subtitle line, optionally tagged with a speaker name" viewport="700x140"
 */
export interface CaptionLineProps {
  text: string;
  speaker?: string;
  size?: "sm" | "md" | "lg";
}
