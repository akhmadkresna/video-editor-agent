# Moved

SFX audio now lives in the **shared framework pack**: [`assets/sfx/`](../../../assets/sfx/).

Evidence no longer ships a separate `camera_shutter` / `paper_page` / `soft_tick`
fork — those kinds resolve from `assets/sfx/pack.yaml` (`shutter`, `paper`, `tick`).
