id: episode-name
sources:
  cam: raw/cam.mp4
  # screen: raw/screen.mp4
style: tutorial
# Composite single-file (OBS cam+screen in one recording):
# composite:
#   enabled: true
#   baked_pip: true
#   camera_play:
#     snap_on_cuts: false
#     home: wide
#     alt: medium
#     scales: { wide: 1.0, medium: 1.08, close: 1.15 }
asr:
  backend: auto
  model: large
  language: id
fps: 30
aspect: "16:9"
width: 1920
height: 1080
# voice_enhance:
#   enabled: false   # opt out of DeepFilterNet cam VO (default on)
